# basic R objects ########

# vector, one dimensional. 
a <- 1
b <- c(1,2,3)
d <- c("a","d")

# matrix, two dimensional, all cols same number of rows
# all cols same data type
m <- matrix(1:20, nrow = 5)

# data.frame, two dimensional (most of the time :), 
# all cols same length
d <- data.frame


# list, allows for nested structure

# Working in a dataset



